<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Datum</th>
      <th>Beschreibung</th>
      <th>Ort</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.id %></td>
        <td><%= link_to event.name, event %></td>
        <td><%= event.date %></td>
        <td><%= event.description %></td>
        <td><%= link_to(Location.find_by(id: event.location).name, event.location) %></td>
        <td><%= link_to 'Details', event %></td>
        <td><%= link_to 'Bearbeiten', edit_event_path(event) %></td>
        <td><%= link_to 'Löschen', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neuer Event', new_event_path %>
